---
layout: layouts/base.njk
title: "Home"
---

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
	<header class="mb-12 text-center">
		<h1 class="text-4xl font-bold mb-4 content-text">
			Welcome to the CKB.tools blog
		</h1>
		<p class="text-xl max-w-2xl mx-auto content-text">
			A simple, elegant blog built with 11ty and Tailwind CSS. Explore thoughts, tutorials, and insights on web development and technology.
		</p>
	</header>

	<section>
		<h2 class="text-2xl font-semibold mb-8 content-text">
			Latest Posts
		</h2>
		
		{% if collections.post and collections.post.length > 0 %}
		<div class="space-y-8">
			{% for post in collections.post | reverse %}
			<article class="border-b border-visible pb-8 last:border-b-0">
				<header class="mb-4">
					<h3 class="text-xl font-semibold mb-2">
						<a href="{{ post.url }}" class="content-text link-green">
							{{ post.data.title }}
						</a>
					</h3>
					<div class="text-sm mb-3 content-text">
						<time datetime="{{ post.data.date | date('Y-m-d') }}">
							{{ post.data.date | date("F j, Y") }}
						</time>
					</div>
					{% if post.data.tags and post.data.tags.length > 0 %}
					<div class="flex flex-wrap gap-2 mb-3">
						{% for tag in post.data.tags %}
						{% if tag != "post" %}
						<span class="tag">
							{{ tag }}
						</span>
						{% endif %}
						{% endfor %}
					</div>
					{% endif %}
				</header>
				
				<div class="mb-4 content-text">
					{% set excerpt = post.templateContent | striptags | truncate(150) %}
					{{ excerpt }}
				</div>
				
				<a href="{{ post.url }}" class="inline-flex items-center font-medium link-green">
					Read more
					<svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
						<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
					</svg>
				</a>
			</article>
			{% endfor %}
		</div>
		{% else %}
		<div class="text-center py-12">
			<p class="text-lg content-text">
				No posts yet. Check back soon for new content!
			</p>
		</div>
		{% endif %}
	</section>
</div>